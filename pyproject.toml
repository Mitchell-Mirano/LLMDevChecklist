[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "llm-dev-checklist"
version = "1.2.0"
description = "Modular Code Quality Audit & Checklist Suite for AI/LLM-generated code, Python, Vue.js, and LaTeX Thesis Projects"
readme = "README.md"
authors = [
    { name = "Merickson Projects Team" }
]
requires-python = ">=3.9"
dependencies = [
    'tomli; python_version < "3.11"',
]

[project.scripts]
devcheck = "devcheck.cli:main"
tesischeck = "tesischeck.__main__:main"
# Backward-compatible aliases
checklist = "devcheck.cli:main"
llm-dev-checklist = "devcheck.cli:main"
thesis-checker = "tesischeck.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["devcheck", "tesischeck"]

[project.urls]
Homepage = "https://github.com/mitchellmirano/LLMDevChecklist"
Repository = "https://github.com/mitchellmirano/LLMDevChecklist.git"
